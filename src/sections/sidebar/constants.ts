export const DRAG_TYPES = {
  CHAT: "chat",
  PROJECT: "project",
  RECENTS: "recents",
} as const;

export const LOCAL_STORAGE_KEYS = {
  HIDE_MOVE_CUSTOM_AGENT_MODAL: "qilegal:hideMoveCustomAgentModal",
} as const;

export const DEFAULT_PERSONA_ID = 0;
